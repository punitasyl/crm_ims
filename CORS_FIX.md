# 🔧 Исправление CORS ошибки

## ❌ Проблема

```
Access to XMLHttpRequest at 'https://crmims-production.up.railway.app/auth/register' 
from origin 'https://crm-ims.vercel.app' has been blocked by CORS policy
```

## ✅ Решение

### Шаг 1: Добавьте CORS_ORIGINS в Railway

1. Откройте Railway Dashboard
2. Выберите ваш веб-сервис (не PostgreSQL)
3. Перейдите в "Variables"
4. Нажмите "+ New Variable"
5. **Name:** `CORS_ORIGINS`
6. **Value:** `https://crm-ims.vercel.app`
   - Если у вас несколько доменов, разделите запятой:
   - `https://crm-ims.vercel.app,https://www.yourdomain.com`
7. Сохраните

### Шаг 2: Перезапустите сервис

Railway автоматически перезапустит сервис после добавления переменной.

### Шаг 3: Проверьте логи

После перезапуска в логах должно быть:
```
🌐 CORS Origins configured: ['https://crm-ims.vercel.app']
```

## 🔍 Дополнительная информация

### Если все еще не работает:

1. **Проверьте формат URL:**
   - Должен быть полный URL с `https://`
   - Без слеша в конце: `https://crm-ims.vercel.app` ✅
   - НЕ `https://crm-ims.vercel.app/` ❌

2. **Проверьте, что переменная установлена:**
   - В логах должно быть: `CORS_ORIGINS in env: ✅ YES`

3. **Если используете несколько доменов:**
   - Разделите запятой: `https://domain1.com,https://domain2.com`
   - Без пробелов между запятыми

### Также проверьте фронтенд:

Убедитесь, что в фронтенде правильный URL API:
- Должен быть: `https://crmims-production.up.railway.app/api/auth/register`
- НЕ: `https://crmims-production.up.railway.app/auth/register` (без `/api`)

Проверьте файл `.env` или настройки Vercel:
```
NEXT_PUBLIC_API_URL=https://crmims-production.up.railway.app
```

## 📝 Примечание

После добавления `CORS_ORIGINS` и перезапуска, CORS должен работать правильно.

