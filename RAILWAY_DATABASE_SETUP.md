# 🔧 Настройка DATABASE_URL в Railway

## ❌ Проблема

В логах видно:
```
📊 Database URL: sqlite:///./crm_ims.db
```

Это означает, что `DATABASE_URL` не установлен, и используется SQLite вместо PostgreSQL.

## ✅ Решение

### Вариант 1: Добавить DATABASE_URL вручную (Рекомендуется)

1. **Откройте PostgreSQL сервис в Railway:**
   - Найдите ваш PostgreSQL сервис
   - Перейдите в "Variables" или "Connect"
   - Скопируйте `DATABASE_URL` (формат: `postgresql://user:password@host:port/dbname`)

2. **Добавьте в веб-сервис:**
   - Откройте ваш веб-сервис (не PostgreSQL!)
   - Перейдите в "Variables"
   - Нажмите "+ New Variable"
   - **Name:** `DATABASE_URL`
   - **Value:** вставьте скопированный `DATABASE_URL`
   - Сохраните

3. **Railway автоматически перезапустит сервис**

4. **Проверьте логи:**
   - Должно быть: `📊 Database URL: postgresql://...`
   - НЕ должно быть: `📊 Database URL: sqlite://...`

### Вариант 2: Использовать Railway Service Reference

1. **В веб-сервисе → "Variables" → "+ New Variable"**
2. **Name:** `DATABASE_URL`
3. **Value:** 
   - Выберите "Reference" (или "Service Reference")
   - Выберите ваш PostgreSQL сервис
   - Выберите `DATABASE_URL`
4. Сохраните

Это автоматически свяжет переменные между сервисами.

## 🔍 Проверка

После настройки проверьте логи:

**✅ Правильно:**
```
📊 Database URL: postgresql://user:password@host:port/dbname
✅ Database tables created successfully!
```

**❌ Неправильно:**
```
📊 Database URL: sqlite:///./crm_ims.db
```

## 🐛 Если все еще не работает

1. **Проверьте формат DATABASE_URL:**
   - Должен начинаться с `postgresql://` или `postgresql+psycopg2://`
   - Формат: `postgresql://user:password@host:port/dbname`

2. **Проверьте, что PostgreSQL сервис запущен:**
   - В Railway → PostgreSQL сервис → должен быть статус "Active"

3. **Проверьте переменные окружения:**
   - В веб-сервисе → "Variables"
   - Убедитесь, что `DATABASE_URL` есть в списке
   - Проверьте, что значение правильное (начинается с `postgresql://`)

4. **Перезапустите сервис вручную:**
   - "Deployments" → "Redeploy"

## 📝 Примечание

Railway **НЕ** автоматически передает `DATABASE_URL` из PostgreSQL сервиса в другие сервисы. Нужно добавить его вручную или использовать Service Reference.

