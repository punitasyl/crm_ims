# 🔧 Troubleshooting: Таблицы не создаются в PostgreSQL

## 🔍 Шаг 1: Проверьте логи Railway

1. Откройте Railway Dashboard
2. Выберите ваш сервис
3. Перейдите в "Deployments" → выберите последний деплой → "View Logs"
4. Ищите сообщения:
   - `🚀 Starting CRM IMS Backend...`
   - `📊 Database URL: ...`
   - `🔨 Creating database tables...`
   - `✅ Database tables created successfully!` или `❌ ERROR: ...`

## 🔍 Шаг 2: Проверьте переменные окружения

В Railway Dashboard → ваш сервис → "Variables":

1. **DATABASE_URL должен быть установлен:**
   ```
   postgresql://user:password@host:port/dbname
   ```

2. **Как получить DATABASE_URL:**
   - В Railway → ваш PostgreSQL сервис → "Connect" → "PostgreSQL"
   - Скопируйте "Connection String" или "DATABASE_URL"
   - Добавьте в переменные окружения вашего веб-сервиса

3. **Проверьте, что DATABASE_URL добавлен в правильный сервис:**
   - Должен быть в **веб-сервисе** (не в PostgreSQL сервисе)
   - Railway может автоматически добавить его, но лучше проверить

## 🔍 Шаг 3: Проверьте подключение к базе данных

Используйте Railway Shell для проверки:

1. В Railway → ваш сервис → "Deployments" → "View Logs" → "Shell"
2. Выполните:
   ```bash
   cd backend
   python check_database.py
   ```

Этот скрипт покажет:
- ✅ Подключение к базе данных
- 📋 Список существующих таблиц
- ❌ Какие таблицы отсутствуют

## 🔍 Шаг 4: Создайте таблицы вручную

Если таблицы не создались автоматически:

1. **Через Railway Shell:**
   ```bash
   cd backend
   python init_postgresql.py
   ```

2. **Или через SQL:**
   ```bash
   # Подключитесь к PostgreSQL через Railway
   # В PostgreSQL сервисе → "Connect" → "PostgreSQL"
   psql $DATABASE_URL -f database/migrations/001_initial_schema.sql
   ```

## 🐛 Частые проблемы

### Проблема 1: DATABASE_URL не установлен

**Симптомы:**
- В логах: `📊 Database URL: sqlite:///./crm_ims.db`
- Таблицы не создаются в PostgreSQL

**Решение:**
1. Убедитесь, что PostgreSQL сервис создан
2. Скопируйте DATABASE_URL из PostgreSQL сервиса
3. Добавьте в переменные окружения веб-сервиса

### Проблема 2: DATABASE_URL в неправильном формате

**Симптомы:**
- Ошибка подключения в логах
- `Connection refused` или `Database does not exist`

**Решение:**
- DATABASE_URL должен начинаться с `postgresql://` или `postgresql+psycopg2://`
- Формат: `postgresql://user:password@host:port/dbname`

### Проблема 3: Ошибка "Module not found: psycopg2"

**Симптомы:**
- В логах: `ModuleNotFoundError: No module named 'psycopg2'`

**Решение:**
- Убедитесь, что `psycopg2-binary>=2.9.9` в `requirements.txt` ✅ (уже добавлено)
- Пересоберите проект в Railway

### Проблема 4: Таблицы создаются, но пустые

**Симптомы:**
- Таблицы существуют, но нет данных

**Решение:**
- Это нормально! Таблицы создаются пустыми
- Создайте администратора: `python create_admin.py`

### Проблема 5: Root Directory не настроен

**Симптомы:**
- Ошибки импорта модулей
- `ModuleNotFoundError: No module named 'app'`

**Решение:**
1. В Railway → ваш сервис → "Settings" → "Root Directory"
2. Установите: `backend`
3. Или используйте команду с `cd backend` в `railway.json`

## ✅ Быстрая проверка

Выполните в Railway Shell:

```bash
cd backend
python check_database.py
```

Если видите:
- ✅ `Connection successful!`
- ✅ `All expected tables exist!`

То все работает! Если нет - следуйте инструкциям выше.

## 📞 Если ничего не помогает

1. **Проверьте логи Railway** - там должна быть подробная информация об ошибке
2. **Запустите `check_database.py`** - покажет точную проблему
3. **Создайте таблицы вручную** через `init_postgresql.py`
4. **Проверьте, что PostgreSQL сервис запущен** в Railway

